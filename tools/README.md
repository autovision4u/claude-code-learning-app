# Tools

Python scripts for deterministic execution. Each script should:
- Do one thing well
- Accept inputs via arguments or stdin
- Load credentials from `.env`
- Print errors to stderr, outputs to stdout
